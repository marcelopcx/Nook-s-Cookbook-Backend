# Copia este archivo a .env:
#   cp .env.example .env
#
# Necesario para cargo run y para Cargo Check del IDE (junto con build.rs).

# Credenciales alineadas con docker-compose.yml
# Debe coincidir con POSTGRES_PASSWORD en docker-compose.yml (ahora: secret123)
DATABASE_URL=postgres://nooks:secret123@127.0.0.1:5432/nooks_cookbook?options=-csearch_path%3Dnooks_cookbook

JWT_SECRET=un_secreto_largo_minimo_32_caracteres_cambiar_en_produccion
JWT_EXPIRATION_HOURS=24

HOST=127.0.0.1
PORT=8080
